require "spec_helper"

describe Selectize::Ajax do
  it "has a version number" do
    expect(Selectize::Ajax::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
